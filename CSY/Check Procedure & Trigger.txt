SELECT TRIGGER_NAME 
FROM USER_TRIGGERS;

SELECT OBJECT_NAME
FROM USER_OBJECTS
WHERE OBJECT_TYPE = 'PROCEDURE';

SELECT 'DROP TRIGGER ' || TRIGGER_NAME || ';'
FROM USER_TRIGGERS;

SELECT 'DROP PROCEDURE ' || OBJECT_NAME || ';'
FROM USER_OBJECTS
WHERE OBJECT_TYPE = 'PROCEDURE';
